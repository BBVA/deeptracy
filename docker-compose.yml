version: '3'

services:
  #deeptracy:
  #  build: .
  #  image: deeptracy
  #  depends_on:
  #    - redis
  #    - postgres
  #  environment:
  #    - REDIS_URI=redis://redis:6379
  #    - POSTGRES_URI=postgresql://postgres:postgres@postgres:5432/deeptracy

  postgres:
    image: postgres:9.6-alpine
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=postgres

  redis:
    image: redis:3-alpine
    ports:
      - 6379:6379
